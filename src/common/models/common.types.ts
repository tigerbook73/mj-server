export enum Position {
  East = "east",
  West = "west",
  North = "north",
  South = "south",
}

export enum PlayerRole {
  Player = "player",
  Observer = "observer",
}

export enum UserType {
  Human = "human",
  Bot = "bot",
}
